# Copiá este archivo como `.env` (en la carpeta backend):
#   copy .env.example .env
# No subas `.env` al repo.

# SQL Server (Docker: database/docker-compose.yml → SA_PASSWORD debe ser la MISMA que password= abajo)
DATABASE_URL="sqlserver://localhost:1433;database=restaurant_db;user=sa;password=MesasDemo1;encrypt=true;trustServerCertificate=true"

JWT_SECRET="cambia-esto-por-un-secreto-seguro-de-al-menos-32-chars"
PORT=3000
NODE_ENV=development
CLIENT_URL=
